
@{
    ViewBag.Title = "Qrcodegen";
}

    <style>
        body {
            margin: 25px;
        }

        div.polaroid {
            width: 100%;
            background-color: white;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
            margin-bottom: 25px;
        }

        div.container {
            text-align: center;
            padding: 10px 20px;
        }
    </style>

<h2>Qrcodegen</h2>

<div class="polaroid">
    @if (ViewBag.Qr != null)
    {
        @:<img src="@Url.Content(ViewBag.Qr)" alt="image" style="width:50%;padding-left:20px" />
    }
    
</div>

